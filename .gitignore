# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Python (build pipeline)
__pycache__/
*.pyc
venv/
.venv/

# Raw / unprocessed art — keep these out of the repo.
# Only processed sprites in assets/sprites/ belong in git.
raw_art/
*_raw/
LittleSprout_Character_Sheet*.zip

# Standalone offline builds (regenerated, not source)
dist/

# Pre-consolidation checkout of the backend, now vendored as rewards-api/.
# Kept on disk until PR #2 merges, then delete it. Ignored so `git add -A`
# can't accidentally stage it as an embedded repo / gitlink.
LittleSprout-Rewards/
